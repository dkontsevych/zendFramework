<h1>Add a Admin  post</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url());
echo $this->partial('admin/write/form', [
    'form' => $form,
    'submitLabel' => 'Insert new post',
]);