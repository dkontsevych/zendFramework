<h1>Add a Computer  post</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url());
echo $this->partial('computer/write/form', [
    'form' => $form,
    'submitLabel' => 'Insert new post',
]);